spring:
  datasource:
    url: jdbc:mysql://${TEST_DB_HOST}:${TEST_DB_PORT:3306}/${TEST_DB_NAME:opencarapace}?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&socketFactory=com.mysql.cj.protocol.StandardSocketFactory
    username: ${TEST_DB_USERNAME}
    password: ${TEST_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8888
